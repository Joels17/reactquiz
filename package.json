{
  "dependencies": {
    "cross-fetch": "^3.1.4",
    "csv-parser": "^3.0.0",
    "express": "^4.17.1"
  },
  "scripts": {
    "start": "node server/index.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": "16.2.0"
  }
}
